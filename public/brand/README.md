# Approved brand runtime slot

`convalt-logo.svg` is intentionally absent until the controlled approved-asset promotion copies the client-approved SVG from `assets-source/approved/`.

Do not place a generated or scraped logo here and set `NEXT_PUBLIC_BRAND_ASSET_SET=approved` without client approval.
