'use client';

import { useEffect, useRef, useState } from 'react';
import * as THREE from 'three';

type SceneSpec={id:string;label:string;kicker:string;title:string;body:string;side?:'left'|'right';camera:[number,number,number];target:[number,number,number];background:number;accent:number};
type Diagnostics={fps:number;averageFps:number;activeScene:number;sceneId:string;drawCalls:number;triangles:number;webgl:boolean;dpr:number;timestamp:number};
declare global{interface Window{__CONVALT_DIAGNOSTICS__?:Diagnostics}}

const scenes:SceneSpec[]=[
{id:'hero',label:'Integrated platform',kicker:'01 — Convalt Energy',title:'Powering a more resilient future.',body:'A cinematic 3D journey through manufacturing, power generation, data infrastructure and circular-energy systems.',camera:[9.6,6.4,15.2],target:[0,1,0],background:0x09100c,accent:0xd29549},
{id:'manufacturing',label:'Solar manufacturing',kicker:'02 — Manufacturing',title:'Built at industrial scale.',body:'A purpose-built solar manufacturing hall reveals production cells, material flow, inspection stations and line infrastructure.',side:'right',camera:[8.2,4.8,11.3],target:[0.8,1.2,0],background:0x0d110e,accent:0xc57a2f},
{id:'generation',label:'Power generation',kicker:'03 — Power generation',title:'Generation meets landscape.',body:'A dedicated energy field combines solar arrays, wind turbines and grid infrastructure in a broad outdoor composition.',camera:[-9.2,5.8,13.8],target:[0,1,0],background:0x09110f,accent:0xd4a45e},
{id:'data',label:'Data centers',kicker:'04 — Data centers',title:'Infrastructure for the digital economy.',body:'A separate data-center environment exposes server aisles, cooling infrastructure and an energy-fed operations spine.',side:'right',camera:[7.5,3.8,10.2],target:[0.4,1.4,0],background:0x080d0c,accent:0xb66f31},
{id:'recycling',label:'Recycling',kicker:'05 — Recycling',title:'Materials return to the system.',body:'The recycling scene visualizes intake, sorting, processing and recovered-material loops as a distinct industrial layout.',camera:[-7.8,4.5,11.8],target:[-0.2,1,0],background:0x0a100c,accent:0xbd7a2d},
{id:'close',label:'Connected ecosystem',kicker:'06 — Connected platform',title:'One connected energy platform.',body:'The closing scene recombines the visual language of all four pillars into a single energy-campus composition.',side:'right',camera:[0.9,6.6,15.5],target:[0,1.3,0],background:0x090d0b,accent:0xd0a05f}
];

const mat=(color:number,metalness=.35,roughness=.68,emissive=0,emissiveIntensity=0)=>new THREE.MeshStandardMaterial({color,metalness,roughness,emissive,emissiveIntensity,transparent:true});
function box(g:THREE.Group,s:[number,number,number],p:[number,number,number],m:THREE.Material,r=0){const x=new THREE.Mesh(new THREE.BoxGeometry(...s),m);x.position.set(...p);x.rotation.y=r;g.add(x);return x}
function cyl(g:THREE.Group,r:number,h:number,p:[number,number,number],m:THREE.Material,n=16){const x=new THREE.Mesh(new THREE.CylinderGeometry(r,r,h,n),m);x.position.set(...p);g.add(x);return x}

function hero(){const g=new THREE.Group(),ground=mat(0x18231c,.1,.95),dark=mat(0x1e2924,.55,.6),glass=mat(0x183847,.7,.3),copper=mat(0xb66f31,.7,.38),green=mat(0x536d59,.2,.86);box(g,[28,.6,22],[0,-.65,0],ground);box(g,[7,2.8,5],[4,1,-3],dark);box(g,[5.8,.08,3.8],[4,2.45,-3],glass);for(let z=-7;z<=7;z+=2.5)for(let x=-10;x<=-2;x+=2.3){const p=box(g,[1.8,.08,1.1],[x,.05,z],glass);p.rotation.x=-.18}for(let i=0;i<6;i++)box(g,[.42,2.3,1.1],[2+i*.75,.75,3.5],dark);for(let i=0;i<7;i++){const a=i/7*Math.PI*2;box(g,[.7,.7,.7],[Math.cos(a)*7,0,Math.sin(a)*7+5],green,a)}for(let i=0;i<5;i++)box(g,[.55,.12,.55],[-1+i*.7,.1,-4.8],copper);return g}
function manufacturing(){const g=new THREE.Group(),floor=mat(0x202824,.15,.86),steel=mat(0x7f8984,.75,.38),dark=mat(0x1b211e,.5,.62),copper=mat(0xb86f2d,.7,.4),glass=mat(0x193842,.65,.32),signal=mat(0xd59b4b,.45,.5,0x5b3510,.5);box(g,[25,.45,15],[0,-.55,0],floor);for(const x of[-10,-6,-2,2,6,10]){box(g,[.22,5,.22],[x,1.8,-6.4],steel);box(g,[.22,5,.22],[x,1.8,6.4],steel)}box(g,[22,.2,.2],[0,4.2,-6.4],steel);box(g,[22,.2,.2],[0,4.2,6.4],steel);for(let i=0;i<10;i++){const x=-8+i*1.75;box(g,[1.45,.2,1.1],[x,.05,0],glass);box(g,[.1,.7,1.35],[x,.38,0],steel)}for(const x of[-6,-2,2,6]){box(g,[1.4,1.4,2.2],[x,.7,-3.2],dark);box(g,[.5,.12,.7],[x,1.45,-3.2],signal)}for(let i=0;i<8;i++)cyl(g,.14,.7,[-7+i*2,0,3.4],copper,12);return g}
function generation(){const g=new THREE.Group(),earth=mat(0x1d2a20,.05,.98),solar=mat(0x173b4b,.72,.28),steel=mat(0x9da4a0,.8,.32),grid=mat(0x39453f,.55,.55),copper=mat(0xc07a35,.65,.4);box(g,[34,.5,24],[0,-.7,0],earth);for(let z=-8;z<=6;z+=2.6)for(let x=-12;x<=1;x+=2.2){const p=box(g,[1.8,.07,1.15],[x,.05,z],solar);p.rotation.x=-.22;p.rotation.y=.05}const turbine=(x:number,z:number,s:number)=>{cyl(g,.11*s,6*s,[x,2.35*s,z],steel,12);const rotor=new THREE.Group();rotor.position.set(x,5.35*s,z);rotor.userData.spin=true;rotor.add(new THREE.Mesh(new THREE.SphereGeometry(.24*s,12,12),steel));for(let i=0;i<3;i++){const b=new THREE.Mesh(new THREE.BoxGeometry(2.5*s,.08*s,.13*s),steel);b.position.x=1.25*s;b.rotation.z=i*Math.PI*2/3;rotor.add(b)}g.add(rotor)};turbine(7,-6,1);turbine(10,1,.9);turbine(6,7,.8);box(g,[5,2.5,3],[8,.55,6],grid);for(let i=0;i<5;i++)box(g,[.28,.9,.28],[6.5+i*.7,.85,4.35],copper);return g}
function dataCenter(){const g=new THREE.Group(),floor=mat(0x181d1b,.1,.9),rack=mat(0x1d2522,.65,.45),frame=mat(0x727a76,.75,.32),led=mat(0x3f6f57,.2,.5,0x57d58a,2.1),copper=mat(0xb96a29,.7,.36),cool=mat(0x283f46,.62,.38);box(g,[24,.45,15],[0,-.6,0],floor);for(const side of[-1,1])for(let i=0;i<7;i++){const x=side*4.1,z=-5.2+i*1.7;box(g,[1.2,2.8,1.25],[x,1,z],rack);for(let y=0;y<5;y++)box(g,[.05,.08,.45],[x-side*.62,.15+y*.48,z],led)}box(g,[2.4,2.8,10],[0,1,0],cool);for(let i=0;i<6;i++)box(g,[.22,.22,8],[-1+i*.4,3,-.5],frame);for(let i=0;i<8;i++)box(g,[.18,.08,.7],[-1.4+i*.4,.05,5.6],copper);return g}
function recycling(){const g=new THREE.Group(),floor=mat(0x232a24,.08,.94),steel=mat(0x777f79,.76,.42),dark=mat(0x202720,.48,.63),orange=mat(0xb96e2d,.62,.44),green=mat(0x45624d,.25,.75,0x244f31,.45),scrap=mat(0x5f655e,.78,.5);box(g,[27,.45,17],[0,-.6,0],floor);box(g,[8,2.8,5],[-5,1,-3],dark);box(g,[7,2.2,4],[5,.7,3],dark);for(let i=0;i<8;i++)cyl(g,.34,.7,[-8+i*1.1,0,2.5],scrap,10);box(g,[11,.35,1.5],[0,.2,0],steel,.12);for(let i=0;i<6;i++)box(g,[.65,.5,.75],[-3+i*1.3,.58,0],orange,i*.1);for(let i=0;i<9;i++){const a=i/9*Math.PI*2;box(g,[.6,.6,.6],[6+Math.cos(a)*3,0,-3+Math.sin(a)*3],green,a)}return g}
function closing(){const g=new THREE.Group(),base=mat(0x1a241e,.1,.94),dark=mat(0x202a25,.55,.58),solar=mat(0x173a48,.7,.3),copper=mat(0xbe7732,.72,.38),green=mat(0x4f6954,.25,.82),steel=mat(0x8c9490,.78,.34);box(g,[30,.5,22],[0,-.65,0],base);box(g,[6,2.4,5],[0,.8,0],dark);for(let x=-11;x<=-4;x+=2.2)for(let z=-7;z<=4;z+=2.4){const p=box(g,[1.7,.07,1.05],[x,.03,z],solar);p.rotation.x=-.2}for(let i=0;i<6;i++)box(g,[.5,2.1,1],[4+i*.7,.65,-2],dark);cyl(g,.1,5.5,[9,2.1,5],steel,12);box(g,[2.1,.07,.12],[10,4.85,5],steel);for(let i=0;i<8;i++){const a=i/8*Math.PI*2;box(g,[.55,.55,.55],[Math.cos(a)*5,0,Math.sin(a)*5+6],green,a)}for(let i=0;i<7;i++)box(g,[.18,.08,.55],[-1.1+i*.36,.08,-3.2],copper);return g}
const builders=[hero,manufacturing,generation,dataCenter,recycling,closing];

export function Stage6Experience(){
  const mount=useRef<HTMLDivElement>(null),chapterRefs=useRef<(HTMLElement|null)[]>([]);
  const[active,setActive]=useState(0),[qa,setQa]=useState(false),[diag,setDiag]=useState<Diagnostics>({fps:0,averageFps:0,activeScene:0,sceneId:scenes[0].id,drawCalls:0,triangles:0,webgl:false,dpr:1,timestamp:0});
  useEffect(()=>setQa(new URLSearchParams(window.location.search).get('qa')==='1'),[]);
  useEffect(()=>{
    const host=mount.current;if(!host)return;
    let renderer:THREE.WebGLRenderer;
    try{renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'})}catch{host.classList.add('fallback');return}
    renderer.setPixelRatio(Math.min(window.devicePixelRatio,1.5));renderer.setSize(innerWidth,innerHeight);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.02;host.appendChild(renderer.domElement);
    const world=new THREE.Scene();world.background=new THREE.Color(scenes[0].background);world.fog=new THREE.Fog(scenes[0].background,12,36);
    const camera=new THREE.PerspectiveCamera(40,innerWidth/innerHeight,.1,100);camera.position.set(...scenes[0].camera);
    world.add(new THREE.HemisphereLight(0xdde4df,0x111713,1.45));const sun=new THREE.DirectionalLight(0xffe6c1,2.8);sun.position.set(8,12,7);world.add(sun);const rim=new THREE.PointLight(scenes[0].accent,11,25);rim.position.set(-6,5,-5);world.add(rim);
    const groups=builders.map((fn,i)=>{const g=fn();g.visible=i<2;world.add(g);return g});
    const bgA=new THREE.Color(),bgB=new THREE.Color(),bg=new THREE.Color(),aA=new THREE.Color(),aB=new THREE.Color(),cA=new THREE.Vector3(),cB=new THREE.Vector3(),tA=new THREE.Vector3(),tB=new THREE.Vector3(),target=new THREE.Vector3();
    let raf=0,last=performance.now(),win=last,winFrames=0,current=60,total=0,totalTime=0,lastActive=-1,lastPublish=0;
    const animate=(now:number)=>{const dt=Math.max(.1,now-last);last=now;winFrames++;total++;totalTime+=dt;if(now-win>=1000){current=Math.round(winFrames*1000/(now-win));winFrames=0;win=now}
      const storyMax=Math.max(1,(scenes.length-1)*innerHeight);const p=THREE.MathUtils.clamp(scrollY/storyMax,0,1),pos=p*(scenes.length-1),from=Math.floor(pos),to=Math.min(scenes.length-1,from+1),local=pos-from,eased=local*local*(3-2*local),activeIndex=Math.min(scenes.length-1,Math.round(pos));
      if(activeIndex!==lastActive){lastActive=activeIndex;setActive(activeIndex)}groups.forEach((g,i)=>{const distance=Math.abs(i-pos);g.visible=distance<1.08;if(!g.visible)return;const w=Math.max(0,1-distance);g.position.y=-(1-w)*.35;g.rotation.y=(i%2?-.03:.03)*(1-w);g.traverse(o=>{if(!(o instanceof THREE.Mesh))return;const ms=Array.isArray(o.material)?o.material:[o.material];ms.forEach(m=>{if(m instanceof THREE.MeshStandardMaterial)m.opacity=Math.max(.08,w)})})});
      groups[2].traverse(o=>{if(o instanceof THREE.Group&&o.userData.spin===true)o.rotation.z-=dt*.00035});cA.set(...scenes[from].camera);cB.set(...scenes[to].camera);camera.position.lerpVectors(cA,cB,eased);tA.set(...scenes[from].target);tB.set(...scenes[to].target);target.lerpVectors(tA,tB,eased);camera.lookAt(target);bgA.setHex(scenes[from].background);bgB.setHex(scenes[to].background);bg.lerpColors(bgA,bgB,eased);if(world.background instanceof THREE.Color)world.background.copy(bg);if(world.fog instanceof THREE.Fog)world.fog.color.copy(bg);aA.setHex(scenes[from].accent);aB.setHex(scenes[to].accent);rim.color.lerpColors(aA,aB,eased);renderer.render(world,camera);
      if(now-lastPublish>=500){const d:Diagnostics={fps:current,averageFps:totalTime?Math.round(total*1000/totalTime):0,activeScene:activeIndex,sceneId:scenes[activeIndex].id,drawCalls:renderer.info.render.calls,triangles:renderer.info.render.triangles,webgl:true,dpr:renderer.getPixelRatio(),timestamp:Date.now()};window.__CONVALT_DIAGNOSTICS__=d;setDiag(d);lastPublish=now}raf=requestAnimationFrame(animate)};
    const resize=()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight);renderer.setPixelRatio(Math.min(devicePixelRatio,1.5))};addEventListener('resize',resize);raf=requestAnimationFrame(animate);
    return()=>{cancelAnimationFrame(raf);removeEventListener('resize',resize);groups.forEach(g=>g.traverse(o=>{if(o instanceof THREE.Mesh){o.geometry.dispose();const ms=Array.isArray(o.material)?o.material:[o.material];ms.forEach(m=>m.dispose())}}));renderer.dispose();if(host.contains(renderer.domElement))host.removeChild(renderer.domElement)}
  },[]);
  return <><header className="site-nav"><a className="brand" href="#hero">CONVALT <b>ENERGY</b></a><nav className="navlinks" aria-label="Primary navigation"><a href="#projects">Projects</a><a href="#team">Team</a><a href="#media">Media</a><a href="#resources">Resources</a><a href="#contact">Contact</a></nav></header><div className="experience"><div className="canvas-shell" ref={mount}><div className="fallback"/></div><div className="veil"/><div className="hud"><span>{String(active+1).padStart(2,'0')} / 06</span><strong>{scenes[active].label}</strong></div><div className={'diag '+(qa?'show':'')}><div>FPS: {diag.fps}</div><div>AVG: {diag.averageFps}</div><div>SCENE: {diag.sceneId}</div><div>DRAWS: {diag.drawCalls}</div><div>TRIS: {diag.triangles}</div><div>DPR: {diag.dpr.toFixed(2)}</div><div>WEBGL: {diag.webgl?'PASS':'WAIT'}</div></div><div className="progress">{scenes.map((s,i)=><button key={s.id} className={i===active?'active':''} aria-label={'Go to '+s.label} onClick={()=>chapterRefs.current[i]?.scrollIntoView({behavior:'smooth'})}/>)}</div><main className="story">{scenes.map((s,i)=><section id={s.id} key={s.id} ref={n=>{chapterRefs.current[i]=n}} className={'chapter '+(s.side==='right'?'right':'')}><div className="copy"><div className="kicker">{s.kicker}</div>{i===0?<h1>{s.title}</h1>:<h2>{s.title}</h2>}<p>{s.body}</p><span className="cue">{i<5?'Scroll to explore':'Continue to project portfolio'}</span></div></section>)}</main></div><section id="projects" className="below"><div className="kicker">Projects</div><h2>Energy infrastructure across four connected pillars.</h2><p>The conventional content layer follows the immersive sequence with fast, accessible information architecture.</p><div className="grid"><article className="card"><small>Manufacturing</small><div><h3>Solar manufacturing</h3><p>Integrated production and domestic supply-chain infrastructure.</p></div></article><article className="card"><small>Generation</small><div><h3>Power projects</h3><p>Utility-scale renewable generation and supporting grid systems.</p></div></article><article className="card"><small>Compute</small><div><h3>Data centers</h3><p>Energy-aligned digital infrastructure.</p></div></article></div></section><section id="team" className="below dark"><div className="kicker">Team</div><h2>Built by operators.</h2><p>Leadership, development, operations and advisors presented through a clean, responsive directory layer.</p></section><section id="media" className="below"><div className="kicker">Media</div><h2>News and announcements.</h2><p>Media, press releases and company updates remain semantic and indexable outside the immersive canvas.</p></section><section id="resources" className="below dark"><div className="kicker">Resources</div><h2>Industry references.</h2><p>Manufacturing, policy, raw-material and supply-chain resources.</p></section><section id="contact" className="below"><div className="kicker">Contact</div><h2>Start a conversation.</h2><p>Contact delivery credentials are configured independently of the public WebGL experience.</p></section><footer className="footer"><span>Convalt Energy — Stage 6</span><span>Six-scene Next.js / Three.js runtime</span></footer></>
}
